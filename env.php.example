<?php
/**
 * Environment Configuration for Live Server
 * 
 * INSTRUCTIONS:
 * 1. Copy this file and rename it to: env.php
 * 2. Fill in your live database credentials below
 * 3. Upload env.php to your live server's /staff/ directory
 * 4. Make sure env.php is in the same directory as config.php
 * 
 * SECURITY NOTE:
 * - Never commit env.php to version control
 * - Keep this file outside public_html if possible
 * - Set file permissions to 600 (read/write for owner only)
 */

// Database Configuration for Live Server
$DB_HOST = 'localhost';  // Usually 'localhost' or your hosting provider's DB host
$DB_NAME = 'your_database_name';  // Your cPanel/hosting database name
$DB_USER = 'your_database_user';  // Your cPanel/hosting database username
$DB_PASS = 'your_database_password';  // Your cPanel/hosting database password

// Application Base Path (usually auto-detected, but you can override)
// $APP_BASE_PATH = '/staff';  // Uncomment and adjust if needed

/**
 * EXAMPLE FOR COMMON HOSTING PROVIDERS:
 * 
 * cPanel / StackCP Hosting:
 * $DB_HOST = 'localhost';
 * $DB_NAME = 'username_staff';  // usually prefixed with your cPanel username
 * $DB_USER = 'username_staff';
 * $DB_PASS = 'your_password';
 * 
 * Hostinger:
 * $DB_HOST = 'localhost';
 * $DB_NAME = 'uXXXXXXX_staff';
 * $DB_USER = 'uXXXXXXX_staff';
 * $DB_PASS = 'your_password';
 */
?>
