<?php

/**
 * Same as common/config/main-local.php (DB only — safe for web + console).
 */
$host = isset($_SERVER['HTTP_HOST']) ? strtolower((string) $_SERVER['HTTP_HOST']) : '';
$path = str_replace('\\', '/', __DIR__);

$isRoadmaster = str_contains($host, 'roadmasterspares.com')
    || str_contains($path, '/home/roady/')
    || str_contains($path, '/8a9d1d19f3/');

$isUltimate = str_contains($host, 'ultimate.co.tz')
    || str_contains($path, '/home/ultimate/');

$isLive = $isRoadmaster
    || $isUltimate
    || str_contains($path, '/public_html/')
    || str_contains($path, '/home/sites/');

if ($isRoadmaster) {
    $db = [
        'class' => \yii\db\Connection::class,
        'dsn' => 'mysql:host=sdb-73.hosting.stackcp.net;dbname=roady_database-35303633fb03',
        'username' => 'roady_database-35303633fb03',
        'password' => hex2bin('63c2a3432e5859452f5e2b2a4f'),
        'charset' => 'utf8mb4',
        'tablePrefix' => 'rmm_',
    ];
    $mailerSingleton = [
        'class' => \yii\symfonymailer\Mailer::class,
        'viewPath' => '@common/mail',
    ];
} elseif ($isLive) {
    $db = [
        'class' => \yii\db\Connection::class,
        'dsn' => 'mysql:host=127.0.0.1;dbname=ultimate_mail_app',
        'username' => 'ultimate_mail_user',
        'password' => 'Baddyman123!',
        'charset' => 'utf8mb4',
    ];
    $mailerSingleton = [
        'class' => \yii\symfonymailer\Mailer::class,
        'viewPath' => '@common/mail',
    ];
} else {
    $db = [
        'class' => \yii\db\Connection::class,
        'dsn' => 'mysql:host=localhost;dbname=mail_app',
        'username' => 'root',
        'password' => '',
        'charset' => 'utf8mb4',
    ];
    $mailerSingleton = [
        'class' => \yii\symfonymailer\Mailer::class,
        'viewPath' => '@common/mail',
        'useFileTransport' => true,
    ];
}

return [
    'container' => [
        'singletons' => [
            \yii\mail\MailerInterface::class => $mailerSingleton,
        ],
    ],
    'components' => [
        'db' => $db,
        'mailer' => \yii\mail\MailerInterface::class,
    ],
];
