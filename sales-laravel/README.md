# sales-laravel (DEPRECATED)

**Do not use for new work.** Sales now runs through [`erp-laravel/`](../erp-laravel/).

`sales.php` boots `erp-laravel` only (dashboard Blade + desk APIs). This folder is kept temporarily as a reference copy and may be removed after Suggest is migrated into `erp-laravel` Domains.

See [`erp-laravel/README.md`](../erp-laravel/README.md).
