# suggest-laravel (DEPRECATED host)

**Do not use for new work.** Suggest now runs through [`erp-laravel/`](../erp-laravel/) (`Domains/Suggest`).

Keep this folder for the React Vite app under `frontend/` (dist assets) until that app is relocated.

`suggest.php` boots `erp-laravel` only.
